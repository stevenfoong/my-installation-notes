# Cloudflare API token with Zone:DNS:Edit permission for the DNS challenge
CF_DNS_API_TOKEN=your-cloudflare-api-token

# Email address registered with Let's Encrypt for certificate notifications
ACME_EMAIL=your-email@example.com

# Fully-qualified domain name for the Traefik dashboard
DASHBOARD_DOMAIN=dashboard-proxy.example.com
